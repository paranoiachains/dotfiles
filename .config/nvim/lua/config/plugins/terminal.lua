require("toggleterm").setup({
    direction = "float",

    close_on_exit = true,

    persist_mode = true,

    start_in_insert = true,

    float_opts = {
        border = "rounded",
        winblend = 0,
    },
})
